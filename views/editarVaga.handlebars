<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Vaga</title>

    <style>
        body {
            background-color: #0a0a0a;
            color: #f5f5f5;
            font-family: 'Segoe UI', Roboto, Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        header {
            width: 100%;
            background-color: #111;
            padding: 30px 0;
            text-align: center;
            box-shadow: 0 2px 12px rgba(0, 0, 0, 0.6);
        }

        header h1 {
            color: #4fc3f7;
            font-size: 2rem;
            margin: 0;
        }

        h2 {
            margin: 40px 0 20px 0;
            color: #ccc;
            font-weight: 500;
        }

        form {
            background: linear-gradient(145deg, #141414, #1c1c1c);
            padding: 30px 40px;
            border-radius: 14px;
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.05);
            display: flex;
            flex-direction: column;
            gap: 20px;
            width: 90%;
            max-width: 400px;
        }

        input[type="text"],
        select {
            width: 100%;
            padding: 12px 15px;
            background-color: #222;
            border: none;
            color: #fff;
            border-radius: 8px;
            font-size: 1rem;
        }

        button {
            background-color: #4fc3f7;
            border: none;
            padding: 12px 0;
            border-radius: 8px;
            color: #000;
            font-weight: bold;
            cursor: pointer;
            transition: .2s;
        }

        button:hover {
            background-color: #81d4fa;
        }

        .voltar {
            margin-top: 20px;
            color: #4fc3f7;
            border: 1px solid #4fc3f7;
            padding: 10px 20px;
            border-radius: 8px;
            text-decoration: none;
            transition: .2s;
        }

        .voltar:hover {
            background-color: #4fc3f7;
            color: #000;
        }
    </style>
</head>

<body>

<header>
    <h1>Editar Vaga</h1>
</header>

<h2>Alterar Dados da Vaga</h2>

<form action="/vagas/{{vaga.id}}/editar" method="POST">
    <input type="text" name="numero" value="{{vaga.numero}}" required>

    <select name="veiculoR">
        <option value="">Sem veículo</option>

        {{#each veiculos}}
            <option value="{{id}}" 
                {{#if (eq id ../vaga.veiculoR.id)}} selected {{/if}}>
                {{modelo}} - {{placa}}
            </option>
        {{/each}}
    </select>

    <button type="submit">Salvar</button>
</form>

<a href="/vagas" class="voltar">← Voltar</a>

</body>

</html>
